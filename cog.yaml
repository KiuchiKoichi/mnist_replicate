build:
  python_version: "3.8"
  python_packages:
    - numpy
    - onnxruntime
predict:
  command: python predict.py
  inputs:
    image:
      type: image
  outputs:
    prediction:
      type: int
